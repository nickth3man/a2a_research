{
  "name": "@a2a/api",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn a2a_research.backend.entrypoints.api:app --host 0.0.0.0 --port 8000 --reload",
    "test": "uv run pytest",
    "lint": "uv run ruff check agents/ core/ entrypoints/ llm/ tools/ workflow/ tests/",
    "typecheck": "uv run mypy agents/ core/ entrypoints/ llm/ tools/ workflow/ tests/"
  }
}
